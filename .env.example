# Optional: when set, the app enables Supabase Auth and live RPC-backed data.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Legacy fallback if your project has not switched to publishable keys yet.
VITE_SUPABASE_ANON_KEY=
